# -*- coding: utf-8 -*-
"""clash_share -- package a 3D Viewer export into a single self-contained HTML
file so non-Revit users (project managers, contractors) can open the model and
walk its clashes in a plain browser, off the network, with nothing to install.

See bundle.py for the packer and the Clash Detection panel README for where this
fits. Part of the documented ``lib/clash_*`` shared-module exception.
"""

from clash_share import bundle  # noqa: F401
